
	#normalized = {}

	# for each state in the dictionary, sum all delegate counts to find total number of delegates
	#summed = sum(map(Counter, statesDefault.values()), Counter())
	#sumdelegates = summed['total']
	#sumDems = summed['dems']
	#sumReps = summed['reps']
	#sumOther = summed['other']
	